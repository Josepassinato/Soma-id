import React from 'react';

/**
 * ConfigWarning was removed — Google Apps Script integration is not in use.
 * Kept as empty component to avoid breaking imports.
 */
export const ConfigWarning: React.FC = () => null;
